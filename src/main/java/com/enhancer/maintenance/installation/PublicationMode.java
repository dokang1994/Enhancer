package com.enhancer.maintenance.installation;

/** Closed atomic publication intent; support must be verified by the adapter. */
public enum PublicationMode {
    CREATE_EXCLUSIVE,
    REPLACE_EXISTING
}
