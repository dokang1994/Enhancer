package com.enhancer.workspace;

public enum WorkspaceSourceKind {
    REPOSITORY_DOCUMENT,
    REPOSITORY_FILE,
    ACTIVE_FILE,
    SELECTED_FILE,
    GIT_STATUS,
    GIT_DIFF,
    DIAGNOSTIC,
    TERMINAL_SESSION,
    RUN_RECORD
}
