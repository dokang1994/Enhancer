package com.enhancer.model;

/** Ordered model-visible data classifications supplied by an already approved caller. */
public enum ModelDataClassification {
    PUBLIC,
    INTERNAL,
    CONFIDENTIAL,
    RESTRICTED
}
